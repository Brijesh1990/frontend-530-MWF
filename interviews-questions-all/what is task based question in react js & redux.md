50 Task-Based Questions in React JS & Redux

1. Build a counter app with increment, decrement, and reset functionality using React hooks.

2. Create a form with controlled inputs (name, email, password) and validate required fields.

3. Build a Todo app using useState with add and delete functionality.

4. Create a toggle theme button (Light/Dark mode).

5. Display a list of users from a static array using .map().

6. Build a reusable Button component with dynamic props (color, size).

7. Create a search filter that filters a list based on user input.

8. Build a modal component that opens and closes on button click.

9. Create a dynamic dropdown component.

10. Build a stopwatch with start, stop, and reset functionality.

11. Create a simple pagination component.

12. Build a password show/hide toggle input field.

13. Create a character counter for a textarea.

14. Build a basic accordion component.

15. Create a simple quiz app with score tracking.

🟡 Intermediate React (16–25)
16. Fetch data from a public API and display it using useEffect.

17. Implement loading and error handling during API calls.

18. Build a debounced search input.

19. Create a custom hook for form handling.

20. Build a multi-step form wizard.

21. Create a reusable table component with sorting functionality.

22. Implement infinite scrolling.

23. Build a drag-and-drop list.

24. Create a dynamic form generator from JSON schema.

25. Optimize a component using React.memo.

🔵 Redux Basics (26–35)
26. Build a counter app using Redux (store, action, reducer).

27. Create a Todo app using Redux with add, delete, and toggle complete.

28. Implement filtering (All / Completed / Active) using Redux state.

29. Build a shopping cart system with add and remove functionality.

30. Implement quantity increment/decrement in cart items.

31. Calculate total cart price using Redux selectors.

32. Persist Redux state to localStorage.

33. Implement authentication state management (login/logout).

34. Create a notification system using Redux.

35. Handle multiple reducers using combineReducers.

🔴 Advanced Redux (36–45)
36. Fetch API data using Redux Thunk and store it in Redux.

37. Handle loading and error states in Redux async actions.

38. Normalize nested API response data in Redux store.

39. Implement optimistic UI updates.

40. Create a global error handler middleware.

41. Build a role-based access control system using Redux.

42. Implement memoized selectors using Reselect.

43. Create a Redux-powered theme switcher.

44. Build a real-time notification system (simulate using setTimeout).

45. Split Redux store into feature slices (Redux Toolkit structure).

🟣 Real-World Project Tasks (46–50)
46. Build an e-commerce product listing page with:

Product filtering

Sorting

Add to cart (Redux)

Cart persistence

47. Create a blog app with:

Create, edit, delete posts

Global state management using Redux

48. Build a task management board (like Trello) with drag-and-drop and Redux state.

49. Create a user dashboard with:

Authentication

Protected routes

Redux state

API integration

50. Build a complete CRUD application using:

React for UI
Redux for state management
Async API handling
Form validation
Loading and error states

🎯 These Questions Test

Component architecture

State management

Async handling

Performance optimization

Reusability

Clean code practices

Real-world problem solving

If you want, I can also provide:

✅ Solutions for selected questions

✅ Mini project with full folder structure

✅ Interview-ready task with evaluation criteria

✅ Redux Toolkit version of these tasks